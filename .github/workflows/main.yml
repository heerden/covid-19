on:
  push:
    branches:
      - master
      - master_za

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up Python
      uses: actions/setup-python@v1
    - uses: yaananth/run-notebook@v1
      with:
        notebook: "Realtime R0.ipynb"
        #params: "PATHTOPARAMS.json"
        isReport: False
        poll: True
    - uses: actions/upload-artifact@v2
      if: always()
      with:
        name: calculated_rt
        path: data/rt_old.csv
